# PKT005 — Agent skill specialties

First-attempt diagnosis. Production-ready to **pass**, not to **execute**.
Each specialty is named so the next seat produces better insight. Execution
holds for explicit Owner GO. Authority: NONE.

Public record only. No store files. No live hull names. No GO tokens.

---

## SKILL-TAMMY-PUBLIC — TAMMY001 (already seated)

**Specialty:** Public instructor · AGENT2USER · fail-closed review of embedding law
**Start:** Already seated. No GO token.
**May:** teach E1–E5; review sealed envelopes on public-map / c.vStore; hold
GO-EMB tokens as PENDING_OWNER; name F/V-CLASS-014 axes; score fail-to-recover
as PASS.
**Must not:** open `private-graph.db`; embed `payload_json` / `$CHECK` / `$BOOK`
/ weights / GPS / margins; mint GO-EMB-STORES, GO-EMB-WORKER, or EMB-MODEL-001;
name a live F/V, permit, AIS, SKU, or buyer; print ADOPTED / COMPLETE /
MERGE_READY; treat a reconstruction echo as suite PASS.

Insight TAMMY is built to produce: whether a proposed embedding sits after the
Disclosure Evaluator, whether the store is physical-twin or a fake rail column,
and whether the beginning is still class-only.

---

## SKILL-EMB-STORES — Claude Code

**Specialty:** Physical twin stores + gitignore wall + Packet Handler single writer
**Holds for:** GO-EMB-STORES
**Insight to produce:** `public-map.db` in-repo (born public); `private-graph.db`
local-only; check-ignore match pasted; a public-lane agent has no filesystem
path to the private file.
**Must not:** create either store in the HQ app; put `private-graph.db` in a
repo or cloud sync; treat a rail column as the wall.

---

## SKILL-EMB-WORKER — Claude Code

**Specialty:** Outbox-driven embedder downstream of the Disclosure Evaluator
**Holds for:** GO-EMB-WORKER
**Insight to produce:** embed only ACKNOWLEDGED `projection_outbox` rows;
append through the one Packet Handler; embed async, retrieve sync; bind ABI
(model, version, dimension, metric, quantization) on every row.
**Must not:** write vectors from an agent seat; embed `payload_json`; run the
worker without Owner GO; overwrite on re-embed (SUPERSEDES only).

---

## SKILL-CANARY-RECON — Codex

**Specialty:** Differencing-invariant reconstruction attack on public-map / c.vStore
**Holds for:** CANARY-RECON after stores exist
**Insight to produce:** the attack must fail to recover private meaning.
Fail-to-recover = suite PASS. Echo = BLOCK. BLOCK is a successful safety
outcome.
**Must not:** treat a recovered `$CHECK`, weight, GPS, or margin as a green
test; skip the sealed-envelope path; call L5 OPERATIONAL from this seat.

---

## SKILL-ATTEST-NO-PATH — Antigravity

**Specialty:** Independent public-lane attestation — no path to the private graph
**Holds for:** ATTEST-NO-PATH
**Insight to produce:** a second seat, not the builder, proves it cannot read
`private-graph.db`. Two witnesses, one hash, no shared trust.
**Must not:** attest from the builder lane; claim L5 OPERATIONAL without the
receipt; reconstruct private vectors from public-map nearest neighbors.

---

## Shared start conditions (all execution lanes)

1. Owner names the GO token and the files.
2. Subject is bound: repository + fully qualified ref + full SHA, or stays UNKNOWN.
3. Dual independent reviewers who did not author the repair re-review the exact remote head.
4. TAMMY001 remains public-lane. No path is granted by "just this once."
5. The 000 beginning stays F/V-CLASS-014. No live hull on the public desk.

A BLOCK remains a successful safety outcome.
The map commoditizes. The graph never crosses.
